@page
@model Disable2faModel
@{
    ViewData["Title"] = "Désactiver l'authentification à deux facteurs (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]<span class="text-primary fw-bold">.</span></h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Cette action désactivera uniquement l'authentification à deux facteurs.</strong>
    </p>
    <p>
        La désactivation de la 2FA ne modifie pas les clés utilisées dans les applications d'authentification. Si vous souhaitez changer la clé
        utilisée dans une application d'authentification, vous devez <a asp-page="./ResetAuthenticator">réinitialiser vos clés.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Désactiver la 2FA</button>
    </form>
</div>

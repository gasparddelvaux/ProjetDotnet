@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Configurer votre 2FA";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]<span class="text-primary fw-bold">.</span></h3>
<div>
    <p>Pour utiliser une application d'authentification, suivez les étapes suivantes:</p>
    <ol class="list">
        <li>
            <p>
                Téléchargez une application d'authentification à deux facteurs telle que Microsoft Authenticator pour
                <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a> ou
                <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a> ou
                Google Authenticator pour
                <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=en">Android</a> ou
                <a href="https://itunes.apple.com/us/app/google-authenticator/id388497605?mt=8">iOS</a>.
            </p>
        </li>
        <li>
            <p>Entrez cette clé <kbd>@Model.SharedKey</kbd> dans votre application d'authentification à deux facteurs. Les espaces et les majuscules n'ont pas d'importance.</p>
        </li>
        <li>
            <p>
                Une fois que vous avez saisi la clé ci-dessus, votre application d'authentification à deux facteurs vous fournira 
                un code unique. Entrez le code dans la case de confirmation ci-dessous.
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="Entrez le code ici"/>
                            <label asp-for="Input.Code" class="control-label form-label">Code de vérification</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="w-100 btn btn-lg btn-primary">Vérifier</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
